linters:
  presets:
    - bugs
    - unused
  enable:
    - gofmt
    - misspell
    - revive
    - stylecheck
  disable:
    # powerenv is not a web server, context is not strictly necessary.
    - noctx
